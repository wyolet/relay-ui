node_modules
.claude/worktrees/
.idea/
.DS_Store
dist
dist-ssr
*.local
# All real env files are local-only — they leak baked-in values into builds.
# Commit .env.development.example as the documented template instead.
.env
.env.*
!.env.*.example
.nitro
.tanstack
test-results/
playwright-report/
.wrangler
.output
.vinxi
__unconfig*
todos.json
tsconfig.tsbuildinfo
.cta.json
.symbol/

# Playwright screenshot scratch
e2e/__shots__/
